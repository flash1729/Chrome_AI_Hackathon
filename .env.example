# Example environment file
# This extension does NOT use .env files
# API keys are managed through the extension UI

# To set up your API key:
# 1. Open the extension popup
# 2. Enter your Gemini API key from https://aistudio.google.com/app/apikey
# 3. Click "Save API Key"

# Your API key will be stored securely in Chrome's local storage
# and is never committed to version control
